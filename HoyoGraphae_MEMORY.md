# HoyoGraphae Project Memory

> 项目内部持续记忆 / Living project memo
> 状态：有效（Active）
> 最后更新：2026-09-05
> 当前已发布基线：`v0.1.0`，提交 `d2472c533262ba4fc53d181c4185de883c5e5073`
> 下一目标版本：`v.1.1`（内置字库与目录化交互）

## 1. 本文件的用途

本文件保存 HoyoGraphae 的长期产品定位、已经确认的决策、当前实现状态、下一版本需求、验收标准和未决事项。它是为了让后续开发者或 AI 在上下文中断后能够继续工作，而不是重新猜测项目方向。

每次开始 HoyoGraphae 开发前，应先阅读本文件；每次完成会改变产品范围、架构、资源清单、许可状态或发布状态的工作后，应在同一个提交中更新本文件。

信息优先级如下：

1. 用户最近一次明确决定；
2. 当前代码、测试与实际资源；
3. 本文件记录的既有决策；
4. `README.md`、`README_en.md` 与其他规划文档；
5. 未经验证的推测。

`upstreams.lock.json` 是上游版本的机器可读真值，`LICENSES/` 和 `THIRD_PARTY_NOTICES.md` 是许可与来源边界的真值。本文件不能替代或放宽它们。

### 更新规则

- 更新顶部日期、当前版本和下一目标版本。
- 已完成事项从“计划”移动到“当前实现”，不要只勾选后遗忘实现位置。
- 新资源必须同时记录：来源、固定提交、贡献者授权、许可、应用内标识、构建产物和测试状态。
- 新增或改变产品决定时，在“决策记录”追加日期与结论。
- 不把猜测写成事实；不确定内容必须标记为“待考据”或“待确认”。
- 不在本文件中记录密码、令牌、私钥、个人机器密钥或游戏解包资源。

## 2. 项目身份与长期定位

**HoyoGraphae** 是一个使用本地 Qt 界面的多功能架空文字应用，长期面向米哈游开发的所有游戏，逐步整合其中的架空文字、人工重建字体、组合式书写系统及其查阅、输入、图片导出和 OCR 能力。

核心功能方向：

1. 内置字体与书写系统资源库；
2. 按游戏和文字体系组织的字形查阅库；
3. 实时打字、屏幕排版和图片导出；
4. 架空文字图片识别；
5. 字体文件查看与用户主动安装；
6. 简体中文和英语界面。

资源获得、授权和质量审核是渐进过程。新的游戏、文字、字体、样例和 OCR 模型在审核后固定到主线；“面向所有米哈游游戏”是持续建设范围，不表示当前版本已经完整覆盖。

### 产品用语

| 场景 | 简体中文界面 | 英语界面 |
| --- | --- | --- |
| 开发商总称 | 米哈游 | HoyoVerse |
| 应用名 | HoyoGraphae | HoyoGraphae |
| 字体管理 | 字体管理 | Font Library |
| 字形查阅 | 字形查阅 | Glyph Browser |
| 实时打字 | 实时打字 | Live Typesetter |
| 图片文字识别 | 图片文字识别 | Image OCR |

HoyoGraphae 是非官方同人项目，与米哈游、HoYoverse 及其关联方没有隶属、赞助或背书关系。游戏及产品名称归各自权利人所有。

## 3. 许可与资源边界

- HoyoGraphae 原创 Qt/Python 应用代码：根目录 MIT License。
- `src/*.glyphs` 及继承的 HoYo-Glyphs 字体/字母资源：`LICENSES/HoYo-Glyphs-NONCOMMERCIAL.txt`，不得被根目录 MIT 重新许可。
- `upstream/Honkai-3rd-II-Martian/`：上游 MIT License，Copyright © 2023 陈湛明。
- HoyoGraphae 名称、标志和图标：Copyright © 2026 Songyuan Wu（吴松原，Etymodes），不包含在应用代码 MIT 许可内。
- 当前字体为人工重建资源，不是游戏解包文件。
- 游戏图标属于下一版必需的界面资产；加入仓库前必须记录来源和可再分发依据。若无法确认可再分发，应使用项目自行绘制且可区分游戏的图标卡片，不直接提交来源不明的图片。

支持开发者区域只保留：

- 陈湛明的微信赞赏图片 `donation-wechat.jpg`；
- Etymodes 的 Zelle：`Peterpig123456@gmail.com`。

## 4. 上游与仓库状态

| 来源 | 固定版本 | 当前集成方式 |
| --- | --- | --- |
| `SpeedyOrc-C/HoYo-Glyphs` | `main@ad0c03ce9792d623c6a6161e5582a01903bd5d97` | 历史继承与根目录 `src/*.glyphs` |
| `SpeedyOrc-C/Honkai-3rd-II-Martian` | `main@89998b86c83e364d970be639917f422b00d6015b` | `upstream/Honkai-3rd-II-Martian/` 完整快照 |

主仓库：`https://github.com/Etymodes/HoyoGraphae`

发布基线：

- `main`：`d2472c533262ba4fc53d181c4185de883c5e5073`
- `develop`：`d2472c533262ba4fc53d181c4185de883c5e5073`
- 旧主线归档标签：`archive/pre-hoyographae-rebuild-20260903`
- 旧 `patch-1` 归档标签：`archive/pre-hoyographae-rebuild-20260903-patch-1`

开发原则：新功能先进入 `develop` 检查；来源、许可、测试和界面验收通过后再更新 `main`。

## 5. 当前版本 `v0.1.0`

### 技术基线

- Python `>=3.11`
- PySide6 `>=6.9,<7`
- Qt Widgets
- fontTools `>=4.59,<5`
- pytest `>=8,<10`
- 字体构建工具链：fontmake `3.12.1`、glyphsLib `6.14.0`、fontTools `4.64.0`
- 入口：`python -m hoyographae`
- 代码包：`src/hoyographae/`

Windows 源码构建已经验证：独立 `.venv`、editable 安装、10 项切片 1 测试通过、桌面 `HoyoGraphae.lnk` 可由 `pythonw.exe -m hoyographae` 启动。

### 已有界面

侧栏目前有五页：

1. 首页：项目介绍、功能摘要和简体中文/英语设置；语言即时生效并由 `QSettings` 持久化。
2. 字体库：用户手动选择本机 `.ttf`/`.otf`；仅当前进程加载。
3. 字形查阅：读取当前字体的 `cmap`，按字符、Unicode 编号或 Unicode 名称筛选并分页显示。
4. 实时打字：普通文本输入、当前字体预览、字号/前景色/背景色/透明背景和 PNG 导出。
5. 图片识别：可以选择和预览图片；没有 OCR 模型，不产生伪识别结果。

### 当前局限

- 应用启动后尚未注册内置字体；字体库旧页仍要求用户手动选择外部编译字体。
- 11 个 `.glyphs` 源已能可重复生成 29 个静态 TTF，但运行时只读注册表尚未接入。
- 字体库和字形页没有按游戏、文字体系组织。
- 字形页是 Unicode 字符表，不是明确的“原字母 ↔ 架空字形”字母表。
- 实时打字没有字体下拉选择、三段式布局或可交互 QWERTY 键盘。
- 崩坏三火星文仍是保留的网页上游实现，尚未移植为 Qt 原生组合式渲染器。
- OCR 仍是占位功能。

## 6. 当前资源清单

仓库当前有 11 个 Glyphs 字体源：

| 游戏 | 文字/字体资源 | 源文件 | 对应现实语言或输入层 | 状态 |
| --- | --- | --- | --- | --- |
| 原神 | 提瓦特通用文字 | `src/Teyvat-Black.glyphs` | 英语 | 已生成 1 个静态字体；待运行时注册 |
| 原神 | 稻妻文字 | `src/Inazuma-Brush.glyphs` | 日语；实际字母映射与罗马字规则需在目录中说明 | 已生成 1 个静态字体；样例待审核 |
| 原神 | 须弥雨林文字 | `src/Sumeru-Scribe.glyphs` | 波斯语 | 已生成 1 个静态字体；样例待审核 |
| 原神 | 赤冠/沙漠文字 | `src/Deshret-Inscription.glyphs` | 古埃及语 | 已生成 1 个静态字体；样例待审核 |
| 原神 | 坎瑞亚文字 | `src/Khaenriah-Sun.glyphs` | 拉丁语 | 已生成 Regular/Chasm 2 个静态字体；样例待审核 |
| 原神 | 枫丹文字 | `src/Font-Ainee.glyphs` | 法语映射待内容复核 | 已生成 1 个静态字体；目录元数据待确认 |
| 崩坏：星穹铁道 | 星穹铁道/黑塔空间站文字 | `src/Star-Rail-Neue.glyphs` | 待内容复核 | 已生成 Sans/Serif 共 10 个静态字体；待编目 |
| 崩坏：星穹铁道 | 仙舟文字 | `src/Xianzhou-Seal.glyphs` | 待内容复核 | 已生成 5 个静态字重；待编目 |
| 崩坏：星穹铁道 | 翁法罗斯文字 | `src/Amphoreus-Circuit.glyphs` | 古希腊语方向待逐句复核 | 已生成 5 个静态实例；待编目 |
| 绝区零 | 甲文字 | `src/ZZZ-A.glyphs` | 待内容复核 | 已生成 1 个静态字体；待编目 |
| 绝区零 | 系统文字 | `src/ZZZ-System.glyphs` | 待内容复核 | 已生成 1 个静态字体；待编目 |
| 崩坏三 | 火星文 | `upstream/Honkai-3rd-II-Martian/` | 汉语拼音（含 `ü` 与声调数字） | 组合式矢量书写系统，不是普通字体 |

这里的“待内容复核”不表示资源不可用，而是禁止在应用中未经考据就填入现实语言名称或例句。

## 7. 下一版本 `v.1.1` 的强制需求

### 当前实施进度

切片 1“首页与语言基础”已发布到 `develop` 提交 `5e3175a12ffdc75451f4ce8edcac8dce6dd11bb1`：

- 新增首页并设为默认入口，侧栏从 4 页扩展为 5 页；
- 新增简体中文/英语语言服务；
- 首页语言下拉框可即时刷新导航及五个页面；
- 使用 `QSettings` 持久化语言选择；
- 现有页面的标题、说明、控件、状态、对话框和 OCR 占位说明已接入统一翻译表；
- 语言状态、持久化、页面切换与 PNG 渲染纳入自动化测试。

当前验证：10 项测试通过，包括离屏 Qt 页面与渲染测试。Windows 11 已验证中文和英文完整显示、即时切换、关闭后语言持久化及桌面快捷方式重启。现有外部字体检查器产生的少量底层异常正文仍为中文；该临时上限将在“内置字体与字体管理重做”切片中一并移除，而不是为即将替换的外部选取流程增加第二套错误结构。

切片 2“可重复字体构建”当前实现：

- `scripts/build_fonts.py` 从 11 个受跟踪 `.glyphs` 源生成 29 个静态 TTF；
- 构建工具版本固定，并由 `requirements-font-build.txt` 和可选依赖组记录；
- 无实例的单 master 字体使用 master 构建模式；
- `Khaenriah-Sun` 构建时保留重叠，不改写原始轮廓；
- `Star-Rail-Neue` 只在临时 Designspace 中补齐 Serif 条件轴范围，源文件保持不变，并生成 Sans/Serif 各 5 个字重；
- 生成清单记录稳定 face ID、文件名、字体家族、样式、字符数和 SHA-256；
- 二次从零构建已与首轮结果逐字节一致；运行时注册留到独立切片。

### 7.1 信息架构

应用默认进入首页，侧栏顺序调整为：

1. 首页 / Home
2. 字体管理 / Font Library
3. 字形查阅 / Glyph Browser
4. 实时打字 / Live Typesetter
5. 图片文字识别 / Image OCR

OCR 在本次迭代保留现有明确占位状态；除非另行提出，本版本不把 OCR 模型接入作为发布阻塞项。

### 7.2 首页与设置

首页必须包含：

- HoyoGraphae 的项目介绍、非官方声明和四类主要能力；
- 当前版本号与已内置游戏/文字数量；
- 简体中文与英语语言设置；
- 必要的项目、来源与许可入口。

语言设置要求：

- 选项为“简体中文”和“English”；
- 选择后当前窗口立即刷新，无须重启；
- 使用 `QSettings` 保存，下次启动沿用；
- 同一语言模式下不再把中英文并排硬编码；
- 所有导航、按钮、标题、状态、对话框、占位文字和错误信息都必须进入统一翻译层；
- 中文界面使用“米哈游”，英语界面使用“HoyoVerse”。

### 7.3 字体必须内置

- 应用正常打开后自动加载本项目已经制作并审核的全部可字体化资源，不要求用户选择外部文件。
- `.glyphs` 继续作为可编辑源；运行时使用构建生成的 `.ttf` 或 `.otf`。
- 编译产物必须被打包为 Python package data，并通过稳定的资源 API 定位，不能依赖当前工作目录。
- 每个字体必须有稳定 `font_id`，界面和测试不能依赖操作系统返回的显示名称。
- 单个字体加载失败时应保留其他字体可用，并在界面给出明确诊断；不能静默回退为系统默认字体。
- 崩坏三火星文保留为 `renderer_type = compositional` 的特殊资源，不能伪装成普通 OpenType 字体。

建议的运行时资源位置：

```text
src/hoyographae/resources/
├── catalog/
│   └── scripts.json
├── fonts/
│   └── <font_id>/...ttf
└── games/
    └── <game_id>/icon...
```

现有 `src/*.glyphs` 在完成迁移与历史核对前不删除或改写。字体构建应由可重复执行的脚本完成，不能手工复制一组无法追溯的二进制文件。

### 7.4 统一游戏与文字目录

字体管理页不再以“选择字体文件”为主流程。页面必须直接显示所有已经确认包含架空文字的米哈游游戏，每个游戏卡片包含：

- 游戏图标；
- 当前界面语言对应的游戏名；
- 该游戏下已收录的全部文字体系；
- 每种文字使用其对应现实语言写成的一行例句；
- 使用内置字体渲染的例句预览；
- “打开字体文件 / Open Font File”按钮。

例句原则：

- 坎瑞亚文使用拉丁语；
- 须弥雨林文字使用波斯语；
- 赤冠/沙漠文字使用古埃及语；
- 其他文字必须完成内容考据后再确定，不得用无意义占位文本冒充游戏语言；
- 每条例句在目录数据中保存原文、转写、中文/英文释义、来源或编写依据以及复核状态；
- 波斯语等从右向左的文本必须正确设置方向，不能倒序显示。

“打开字体文件”按钮的确定行为：

- 打开该文字对应的后台 `.ttf`/`.otf` 文件，交由 Windows 字体预览器显示，用户可在系统界面中主动安装；
- 应用不得静默安装系统字体，也不得自行绕过 Windows 权限提示；
- 应用内置预览不依赖系统安装，用户不安装也能完整使用应用；
- 对火星文等非字体资源禁用该按钮，并解释“该书写系统由应用原生渲染，没有可安装字体文件”。

### 7.5 字形查阅

字形查阅首页同样按“游戏 → 文字/字体”列出游戏图标、游戏名和全部字体。点击某个文字后进入其字母表详情。

详情页必须包含：

- 文字名、所属游戏、对应现实语言、字体版本和变体说明；
- 完整字母表或已知符号表；
- 滑动开关切换两个方向：
  - 正查：拉丁字母/输入符号 → 架空字形；
  - 反查：架空字形 → 拉丁字母/输入符号；
- 两个方向改变的是信息主次、排列和可检索侧，不只是修改开关文字；
- 变体字体可以切换变体；缺失字符必须明确显示“未收录”，不能用系统字体假装存在；
- 返回文字列表时保留当前游戏位置。

对于不以简单拉丁字母一一替换的系统，需要由目录声明映射类型。火星文应进入专用的音节/部件查阅视图，不强行塞入 A–Z 网格。

### 7.6 实时打字

实时打字页采用纵向三个主要区域。

#### 上层：架空文字显示与导出

- 显示选定字体渲染的最终文本；
- 顶部提供内置字体下拉选择，按游戏分组或在项目中同时显示游戏名和文字名；
- 保留字号、文字颜色、背景颜色、透明背景和 PNG 导出；
- 字体切换后立刻重绘，不丢失已经输入的文字；
- 导出的图片必须与上层显示内容、换行、颜色、背景和所选字体一致。

#### 中层：可视输入

- 使用普通界面字体显示用户实际输入的字符；
- 保留可见光标、选择、复制、粘贴、退格和换行等正常编辑能力；
- 该区域永远不使用架空字体，以便用户确认自己输入的原字符；
- 输入变化实时同步到上层。

#### 下层：可交互 QWERTY 键盘

- 至少包含 `QWERTYUIOP`、`ASDFGHJKL`、`ZXCVBNM` 三行字母键，以及 Space、Backspace 和 Enter；
- 每个字母键的主要键帽字形使用当前选中的架空字体；必要时以较小普通字体辅助标出实际输入字母；
- 点击虚拟键会向中层光标位置输入相应字符；
- 按下和松开实体键盘时，对应虚拟键显示可见的按压/回弹动画；
- 鼠标按下虚拟键时使用同一套动画状态；
- 切换字体后所有键帽立即更新；
- 不受支持的字母必须显示缺失状态，不能静默显示系统回退字形；
- 快捷键（如 `Ctrl+C`、`Ctrl+V`）仍按文本编辑习惯工作，不应被字母按键动画破坏。

### 7.7 目录数据模型

游戏、文字、字体、语言、例句和字母映射必须从集中式目录读取，不能散落硬编码在多个 QWidget 中。建议每条文字记录至少具有：

```text
game_id
game_name.zh_CN / game_name.en_US
game_icon
script_id
script_name.zh_CN / script_name.en_US
base_language.zh_CN / base_language.en_US
sample.text / sample.transliteration / sample.translation
sample.source / sample.review_status / sample.text_direction
renderer_type (font | compositional)
font_id / font_files / variants
alphabet_mapping
installable
license_id / provenance
```

数据解析必须验证唯一 ID、资源存在性、重复映射、例句复核状态、文本方向和许可标识。未通过验证的数据不能进入发布构建。

## 8. `v.1.1` 验收标准

发布前至少满足：

- [ ] 应用启动后默认显示首页，侧栏共有 5 个入口。
- [ ] 简体中文/英语切换立即生效并能跨重启保存。
- [ ] 所有可字体化的已审核资源从应用包内自动加载。
- [ ] 不选择外部字体也能使用字体管理、字形查阅和实时打字。
- [ ] 字体管理按游戏分组，具有图标、双语名称、全部已收录文字和经复核例句。
- [ ] 可安装字体的按钮能打开正确后台文件；非字体系统给出解释。
- [ ] 字形详情可在正查与反查之间切换，且显示逻辑真正反转。
- [ ] 实时打字呈上、中、下三区，字体选择不会清空输入。
- [ ] 鼠标虚拟键和实体 QWERTY 键都有同步按压动画。
- [ ] PNG 导出与上层预览一致，并支持透明背景。
- [ ] 字体缺字不会被系统字体静默替代。
- [ ] OCR 页继续明确说明真实状态，不伪造结果。
- [ ] Windows 11、Python 3.11 源码安装路径下全套自动化测试通过。
- [ ] README 的“当前可用/计划功能”、版本号、截图、来源和许可同步更新。
- [ ] `HoyoGraphae_MEMORY.md` 与最终实现状态同步。

建议新增测试：

- 目录 schema 与全部资源路径验证；
- 所有内置字体加载及稳定 ID 验证；
- 应用启动时内置字体数量验证；
- 中英切换与 `QSettings` 持久化；
- 游戏/文字分组数量和排序；
- 正查/反查映射一致性；
- 实体键盘与虚拟键输入、按压状态；
- 切换字体后输入保留；
- PNG 金图或关键像素/尺寸回归；
- 字体文件打开行为的 Windows 平台适配层测试。

## 9. 建议实施顺序

1. 建立集中式游戏/文字目录与校验器，完成名称、语言、例句和图标来源审计。
2. 建立 `.glyphs → .ttf/.otf` 可重复构建流程，生成并验证内置字体。
3. 重构 `FontStore` 为启动时加载的只读内置资源注册表，同时保留明确的错误诊断。
4. 建立统一国际化服务与 `QSettings`，实现首页和即时语言切换。
5. 重做字体管理页和字形查阅的游戏/文字层级。
6. 重做实时打字三段式布局和 QWERTY 键盘事件模型。
7. 接入火星文 Qt 原生渲染接口；如果未达到回归标准，明确标记为后续版本，不用普通字体替代。
8. 完成自动化测试、Windows 实机验收、双语 README 和发布记录。

## 10. 本轮不应偷偷扩张的范围

- 不在没有专用模型、冻结测试集和准确率报告时宣称 OCR 已实现。
- 不把游戏解包字体、贴图或来源不明图标混入人工重建字体库。
- 不自动安装或删除 Windows 系统字体。
- 不在运行时修改 `.glyphs` 源文件。
- 不为了复用火星文网页代码而把完整浏览器运行时嵌入 Qt；目标仍是 Qt 原生渲染。
- 不用未经核实的现实语言例句填满界面。
- 不让根目录 MIT 覆盖字体的非商业许可或其他独立许可。

## 11. 待确认与内容审计队列

以下事项必须在相应功能合入前完成，不应凭文件名直接断言：

- 米哈游全部游戏中“确有架空文字”的最终游戏清单及排序。
- 每个游戏图标的来源、使用方式和可再分发状态。
- 枫丹、星穹铁道通用、仙舟、翁法罗斯、绝区零甲/系统文字对应现实语言及例句。
- 每个字体的字母、数字、标点、变体轴、缺字和推荐示例范围。
- `Khaenriah Sun` 的 Regular/Chasm 变体在 UI 中的准确命名。
- 可变字体源（如 Amphoreus、Star Rail、Xianzhou）的发布实例策略。
- 火星文的 Qt 原生音节组合、声调、特殊符号和金图测试集。

## 12. 决策记录

### 2026-09-03

- 项目从旧 fork 重建并正式定位为 HoyoGraphae。
- 原创应用代码采用 MIT；字体资源继续遵守独立非商业许可。
- 继承 HoYo-Glyphs 与 Honkai-3rd-II-Martian 的固定上游历史/快照。
- 建立 Python 3.11 + PySide6 + Qt Widgets 的本地桌面 MVP。
- `main` 与 `develop` 发布到 `d2472c533262ba4fc53d181c4185de883c5e5073`，旧分支状态用归档标签保留。

### 2026-09-05

- Windows editable 源码构建和桌面快捷方式安装完成；5 项测试通过。
- 下一版本定为内置字库与目录化交互迭代，版本记为 `v.1.1`。
- 新增首页，并把简体中文/英语设置放入首页；设置即时生效并持久化。
- 字体管理和字形查阅改为按游戏与文字体系组织，不再依赖用户先选择字体。
- 所有已制作、审核、许可允许打包的字体随应用内置，同时提供用户主动打开后台字体文件的入口。
- 字形查阅加入正查/反查方向切换。
- 实时打字改为显示导出、普通文字输入、可交互 QWERTY 键盘三个纵向区域。
- 实体键盘与虚拟键盘需要共享按压动画和输入状态。
- `v.1.1` 切片 1 发布到 `develop`：默认首页、五页双语即时切换和 `QSettings` 持久化。
- Windows 实机完成切片 1 验收：中英文无缺字、全页即时切换、关闭后语言选择保留。
- 建立切片 2 字体构建门禁：11 个 Glyphs 源可重复生成 29 个带校验清单的静态 TTF；不在构建时改写源文件。
